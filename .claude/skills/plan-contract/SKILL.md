---
name: plan-contract
description: 과제의 실행 계약(기획서 PLAN.md)을 대화로 완성한다. 빈칸을 대신 채워 주지 않고 묻는다 — 문제·AI 역할·범위·Phase별 완료 기준·검증 게이트를 순서대로 확정하고, 마지막에 고치지 않고 지적만 남긴다. PLAN.md는 제출 폼 문항과 같은 순서·같은 제목이라 위에서부터 그대로 옮겨 적으면 된다. "기획 도와줘", "기획서 쓰자", "오늘 뭐 만들지 정하자", "실행 계약 쓰자", "주제 좀 잡아줘", "아까 끊긴 기획 이어서", "PLAN.md 이어서 하자", "제출 전에 기획서 반박해줘"처럼 과제 기획을 시작·재개하거나 기획서를 검토받을 때 사용한다.
---

# 실행 계약 쓰기

기능 목록이 아니라 **실행 계약**을 만든다. 계약이 촘촘하면 구현은 Claude가 한다.
결과물은 `PLAN.md` 한 장이다. 이 파일은 **제출 폼 문항과 같은 순서·같은 제목**으로 쓴다 —
다 쓰고 나면 위에서부터 한 칸씩 그대로 옮겨 적으면 제출이 끝난다.

## 두 시간을 헷갈리지 않는다

| | 시간 | 무엇 |
|---|---|---|
| 기획 대화 | **60분** | 지금 이 스킬로 계약을 쓰는 시간 |
| 구현 | **6~8시간** | 코칭 전후와 저녁, 다음 날 발표 전까지 |

컷오프는 **기획 대화**에만 건다. 계약 쓰는 데 시간이 밀렸다고 과제 범위를 깎지 않는다.
6~8시간짜리 과제를 잡는 것이 맞다 — 60분 안에 만들 수 있는 것을 찾는 자리가 아니다.
사용자가 범위를 너무 작게 잡으면 오히려 "구현에 6~8시간이 있습니다"라고 알려 준다.

## 규칙

**대신 채우지 않는다.** 빈칸은 나중에 Claude가 혼자 정해버리는 자리다.
사용자가 말하지 않은 내용을 추측해서 쓰지 않는다. 사용자의 말을 옮겨 적을 뿐이다.

**한 번에 한 항목.** 항목을 몰아서 묻지 않는다. 한 항목 안의 짝(시간과 횟수, 기계 검증과 사람 검증,
맡길 것과 승인할 것)은 한 번에 물어도 된다. 답을 받고 합격 기준으로 판정한 뒤 다음으로 간다.

**되묻기는 2회까지.** 두 번 되물어도 확정되지 않으면 그 항목에 정확히 `미정 — 1:1 코칭에서 확정`이라고 쓴다.
내가 만든 문장을 대신 넣지 않는다. 그럴듯한 문장이 한 줄 들어가는 순간
그것이 구현의 기본값이 된다. 미정은 미정으로 남겨야 코칭에서 잡힌다.

**빈칸을 남기지 않는다.** 아직 안 정한 항목은 비워 두지 말고 `(아직 안 정함)`이라고 적는다.

**파일은 항상 폼 순서로 쓴다.** 묻는 순서는 아래 단계를 따르되, `PLAN.md`에 적을 때는
언제나 템플릿의 문항 순서를 지킨다. 사용자가 중간에 멈춰도 파일만 열면 제출할 수 있어야 한다.

**구현하지 않는다.** 이 스킬이 도는 동안에는 코드를 만들지 않는다. 계약이 끝나면 거기서 멈춘다.

**이어서 하기.** 시작할 때 `PLAN.md`가 있으면 읽고 **덮어쓰지 않는다.**
채워진 항목은 다시 묻지 않고, "여기까지 돼 있습니다"로 요약한 뒤 첫 미정 항목부터 이어간다.

## 시작하기 전에

**먼저 데이터 원칙부터 말한다.** 첫 질문을 던지기 전에 이 한 문단을 그대로 전한다.

> 이 대화에는 사내 실데이터·고객명·시스템 계정·키를 적지 마세요. 과제도 그것 없이 되게 잡습니다.
> 실제 자료가 필요한 일이면 형식만 같은 샘플 몇 줄을 직접 만들어 씁니다.

대화 중 사용자가 실데이터나 자격증명을 붙여 넣으면 즉시 멈추고, 그 부분을 계약에 옮기지 않은 채
샘플로 바꿔 달라고 요청한다.

**시작 시각을 기록한다.** `date`를 한 번 실행해 시작 시각을 잡고 `PLAN.md` 맨 아래에
`기획 시작 HH:MM`을 주석으로 남긴다. 단계가 바뀔 때마다 `date`로 경과를 확인한다.

| 경과 | 이 시점에는 | 넘겼으면 |
|---|---|---|
| 15분 | 1단계가 끝나 있어야 한다 | 남은 항목을 `미정`으로 두고 2단계로 |
| 30분 | 2단계가 끝나 있어야 한다 | 같은 방식으로 3단계로 |
| 50분 | 3단계가 끝나 있어야 한다 | 같은 방식으로 4단계로 |
| 60분 | 4단계까지 끝 | 지금 상태로 제출 |

컷오프에서는 "지금 15분이 지나 다음으로 넘어갑니다. 이 항목은 미정으로 두겠습니다"라고
알리고 넘어간다. 동의를 기다리지 않는다 — 기다리면 또 늦어진다.
**넘어가는 것은 질문이지 과제 범위가 아니다.** 이 표를 이유로 Phase를 지우거나 범위를 깎지 않는다.

시작 안내 한 줄: "네 단계로 묻겠습니다. 답은 직접 정하셔야 합니다 — 제가 채우면 그 자리가 나중에 문제가 됩니다."

## PLAN.md 템플릿

1단계가 끝나면 이 형태로 파일을 만들고, 이후 단계마다 해당 칸을 채운다.
구분선 위쪽이 폼에 그대로 옮길 부분이고, 아래쪽은 내 작업 메모다.

    # (프로젝트 이름)

    **Capstone 트랙**: 트랙 A — 풀스택 웹앱 / 트랙 B — 자동화 Agent · Workflow
    **구현에 쓸 수 있는 시간**: 코칭 전후·저녁 + 다음 날 발표 전까지 (실질 6~8시간)

    ## ① 문제 정의 — 지금 무엇이 불편한가
    - 누가 / 언제 / 어떤 일에서:
    - 줄이는 비용 (반복 / 대기 / 검증 중 하나):
    - 지금 걸리는 시간·횟수:

    ## ② AI 에이전트에게 맡길 부분
    - 에이전트가 대신할 일:
    - 사람이 계속 할 일:
    - 쓸 도구 (서브에이전트·MCP·스킬·hook·병렬 중 필요한 것만):

    ## ③ 범위 — 반드시 / 되면 좋은 / 안 하는 것
    - 반드시 (이게 안 되면 실패):
    - 되면 좋은:
    - 이번엔 안 하는 것:

    ## ④ Phase 분할과 각 Phase 완료 기준
    1. ...  — 완료: (기계가 판정하는 조건)
    2. ...  — 완료:
    3. ...  — 완료:

    ## ⑤ 완료를 판정할 방법 (검증 게이트)
    - 기계가 판정하는 것:
    - 사람이 눈으로 보는 것:
    - 내가 직접 승인할 지점:

    ## GitHub 저장소 URL

    ## 1:1 코칭에서 가장 묻고 싶은 것

    ---
    (아래는 폼에 넣지 않는 작업 메모)

    ## 되돌아갈 지점과 데이터
    - 되돌리는 방법:
    - 실데이터·자격증명 없이 되게 만드는 방법 (샘플로 대체할 부분):

    ## 고칠 곳 (4단계 반박 결과)

## 1단계. 문제와 비용 → ①

1. "어떤 업무를 대상으로 하시겠어요? 지금 그 일을 어떻게 하고 계신지 두세 문장으로 알려주세요."

   **대상이 안 떠오른다고 하면** 답을 만들어 주지 말고 아래 셋을 대신 묻는다.
   - 지난주에 두 번 이상 똑같이 반복한 일이 무엇이었나요
   - 남이나 시스템의 응답을 기다리느라 멈춰 있던 일은요
   - 결과가 맞는지 눈으로 확인한 일은요

   나온 것 중 하나를 고르게 한다. 셋 다 안 나오면 "가장 최근에 귀찮았던 일 하나"로 좁힌다.

2. 아래 표를 보여주고 **하나만** 고르게 한다. 함께 "지금 걸리는 시간·횟수"도 묻는다(어림수면 된다).

| 비용 | 증상 | 줄었다는 신호 |
|---|---|---|
| 반복 | 매번 손으로 같은 순서를 밟는다 | 명령 한 번으로 같은 결과가 나온다 |
| 대기 | 사람·시스템의 응답을 기다린다 | 기다리는 동안 다음 일이 준비돼 있다 |
| 검증 | 맞게 됐는지 눈으로 확인한다 | 기계가 통과·실패를 대신 판정한다 |

합격 기준 — ①에 **누가·언제·무엇을**이 들어 있고, 비용이 **하나**다.
둘 이상 고르면: "셋 다 줄이면 6~8시간에 어느 것도 끝나지 않습니다. 지금 가장 아픈 것 하나만 고르세요."

여기까지 나오면 위 템플릿으로 `PLAN.md`를 만든다.

## 2단계. 트랙과 범위 → 트랙 · ③ · 프로젝트 이름

1. 트랙을 고르게 한다. 교안의 갈래와 이렇게 이어진다.

| 교안 갈래 | 결과물 | 폼에서 고를 것 |
|---|---|---|
| ① 하네스 | `CLAUDE.md`·`.claude/`·백로그 | 트랙 B |
| ② 도구 — 웹으로 만든다 | 브라우저에서 열리는 화면 | 트랙 A |
| ② 도구 — CLI·스크립트로 만든다 | 명령으로 실행되는 프로그램 | 트랙 B |

2. "6~8시간 뒤에 **반드시** 되어 있어야 하는 것은 무엇인가요?"
3. "되면 좋지만 없어도 되는 것은요?"
4. "이번엔 **안 하는 것**은요?"
5. 마지막에 "이걸 한 줄로 부르면 뭐라고 할까요?" — 프로젝트 이름.

범위 판정은 **6~8시간 기준**이다. 아래가 보이면 조정을 제안하되, 무엇을 옮길지는 사용자가 고른다.

- **너무 크다** — "반드시"에 독립된 기능이 셋 이상이거나, 6~8시간에 1번 Phase도 안 끝날 규모
- **너무 작다** — 30분이면 끝날 것만 "반드시"에 있다. 이때는 "되면 좋은"에서 하나를 끌어올리자고 제안한다
- **막힌다** — 실데이터나 사내 자격증명이 있어야 첫 화면이 뜬다

세 번째면 **로컬 파일 기반**으로 바꾸고 형식만 같은 샘플을 직접 만들자고 안내한다.
사용자가 "실데이터를 쓰겠다"고 하면 계약에 옮기지 않고 다시 제안한다 — 이건 협상 대상이 아니다.

## 3단계. Phase와 검증, AI 역할 → ④ · ⑤ · ②

Phase를 **3~5개**로 쪼개게 하고, 각 Phase마다 "완료:" 문장을 받는다.
여기가 이 한 시간의 핵심이다. 다른 데서 아껴서라도 여기에 시간을 쓴다.
Phase 하나가 한두 시간 분량이면 적당하다 — 6~8시간을 나눈 크기다.

판정 기준 — **사람이 아니라 결과가 판정하는 문장인가.**

| 받는다 | 돌려보낸다 |
|---|---|
| `report.md` 파일이 생긴다 | 정상 동작한다 |
| 3건 중 2건이 NG로 분류된다 | 잘 된다 |
| 명령 한 번으로 표가 출력된다 | 사용자가 만족한다 |

돌려보낼 때 예를 하나 보여줘도 되지만 **반드시 그 사람 소재가 아닌 다른 소재로** 든다.
그 사람 소재로 예를 들면 그대로 "그걸로 할게요"가 되어, 결국 내가 대신 채운 것이 된다.
문장의 **모양**만 보여주고 내용은 사용자가 채운다.

이어서 ⑤와 ②를 채운다.

- **⑤ 검증 게이트** — 기계가 판정하는 것(테스트·lint·빌드·스크립트) / 사람이 눈으로 보는 것 /
  내가 직접 승인할 지점. "완료:"에는 기계 판정만 쓰고 사람이 보는 것은 여기에 적는다. 둘을 섞지 않는다.
  승인 지점이 하나도 없으면 다시 묻는다 — 사람이 판단하는 승인은 있어야 정상이다.
- **② AI 역할** — 에이전트가 대신할 일 / 사람이 계속 할 일 / 5일간 배운 것 중 쓸 도구.
  배운 기능을 다 넣지 않는다. 이 문제에 필요한 것만 고르고 왜 그것인지 한 줄로 적게 한다.

작업 메모 두 줄도 채운다.

- **되돌아갈 지점** — 먼저 이 폴더가 git 저장소인지 확인한다. 저장소면 시작 커밋을 남기고,
  아니면 사용자에게 물어 둘 중 하나를 **내가 실행한다**: git으로 초기화하고 첫 커밋을 만들거나,
  폴더 사본을 하나 떠 둔다. 사용자에게 터미널 명령을 치게 하지 않는다.
- **데이터** — 실데이터·자격증명 없이 되게 만드는 방법, 샘플로 대체할 부분.

## 4단계. 반박 → 고칠 곳 · 코칭 질문

여기서 **작성자 모드를 끈다.** 지금부터는 이 계약을 처음 보는 검토자로 읽는다.

세 가지만 본다.

1. **범위** — 6~8시간 안에 "반드시"가 끝나지 않을 근거, 혹은 반대로 너무 헐거운 근거
2. **검증 불가 조건** — "완료:" 중 사람 판단이 필요한 것
3. **빠진 승인 지점** — Claude가 되돌리기 어려운 일을 승인 없이 하게 되어 있는 곳

지적을 `PLAN.md`의 "고칠 곳"에 목록으로 적는다. 각 지적에 어느 항목인지 붙인다.
지적이 없으면 없다고 적는다 — 억지로 만들지 않는다.

**여기서 끝낸다. 고치지 않는다.** 무엇을 받아들일지는 사용자가 정하고, 반영은 1:1 코칭에서 한다.
고쳐 주겠다고 먼저 제안하지 않는다. 사용자가 명시적으로 "이건 지금 고치자"고 말한 항목만 고친다.

그다음 "1:1 코칭에서 가장 묻고 싶은 것"을 묻는다. 고칠 곳 목록과 미정 항목을 보여주면
사용자가 고르기 쉽다. 여기서도 대신 쓰지 않는다.

> 같이 쓴 사람이 하는 검토라 후하게 볼 수밖에 없다는 점은 사용자에게 그대로 말한다.
> 더 독립적인 시선을 원하면 서브에이전트에게 같은 세 가지를 물어보라고 안내한다 — 선택이고, 토큰을 더 쓴다.

## 마무리

`PLAN.md`를 열어 구분선 위쪽을 그대로 읽어 주고 아래를 확인한다.

- [ ] 트랙과 프로젝트 이름이 정해졌다
- [ ] ①에 누가·언제·무엇을과 비용 하나가 들어 있다
- [ ] ③의 "반드시"가 6~8시간 크기이고, 안 하는 것이 적혀 있다
- [ ] ④의 Phase가 3~5개이고 "완료:"가 전부 기계가 판정하는 문장이다
- [ ] ⑤에 승인 지점이 있다
- [ ] 고칠 곳 목록이 파일에 남았다

미정 항목이 있으면 목록을 읽어 주고 "코칭에서 이것부터 여쭤보세요"라고 알린다.

마지막 안내는 이 한 줄이다.

> `PLAN.md`의 구분선 위쪽이 제출 폼 문항과 같은 순서·같은 제목입니다.
> 위에서부터 한 칸씩 그대로 옮겨 붙이시면 됩니다. **다 채우지 못했어도 지금 상태로 제출**하세요.

사용자가 원하면 각 문항을 하나씩 끊어서 출력해 복사하기 쉽게 해 준다.
**재배열해서 보여줄 뿐 내용을 새로 지어내지 않는다.**
