# SPEC — TV 화면 불량 검사 이력 관리 웹앱

**기반 문서:** BRIEF.md
**작성일:** 2026-08-13
**상태:** 초안 (Draft)

---

## 1. 목적

TV 화면 품질 검사자가 White/Red/Green/Blue/Black 5가지 단색 화면으로 육안 검사한 결과를, 별도 시트나 수기 기록 없이 웹 브라우저에서 바로 입력·저장하고, 저장된 TV 목록과 TV별 과거 검사 이력을 빠르게 조회할 수 있게 한다. 이번 구현은 완벽한 기능보다 "검사 입력 → 저장 → TV 목록 조회 → TV별 이력 조회"라는 핵심 흐름이 실제로 동작하는 시연 가능한 상태를 최우선으로 한다.

---

## 2. 화면 흐름

검사자가 첫 화면부터 저장까지 밟는 순서는 다음과 같다.

1. **검사 입력 화면 진입** — 모델명, 시리얼번호를 입력하고, 검사자는 "검사자 등록" 화면(§6 결정 기록, 2026-08-14)에 미리 등록된 명부 중에서 선택한다(이름+사번으로 표시). 로그인 계정은 여전히 없으며, 목록에 없는 검사자는 "검사자 등록" 화면에서 먼저 추가해야 선택할 수 있다.
2. **화면별 판정 입력** — White → Red → Green → Blue → Black 순서로 5개 화면 각각에 대해 OK/NG를 선택한다.
   - NG를 선택한 화면에서만 불량 항목(복수 선택)과 조치 메모 입력란이 나타난다.
   - OK인 화면은 불량 항목/메모 입력이 필요 없다.
3. **저장 시도** — "저장" 버튼을 누르면 서버가 §4 제약을 검사한다.
   - 실패 시: 어떤 화면·어떤 항목이 문제인지 화면에 표시하고, 입력값은 유지된 채 저장되지 않는다.
   - 성공 시: 검사 일시가 자동 기록되고 저장 완료 메시지가 표시된다.
4. **검사 TV 목록으로 이동** — 저장 후 (또는 메뉴를 통해) TV 목록 화면으로 이동한다. 목록에는 모델명, 시리얼번호, 최근 검사일, 최근 검사 결과(OK/NG), 검사자, 총 검사 건수가 표시된다.
5. **TV 선택** — 목록에서 TV(시리얼번호) 한 행을 클릭한다.
6. **TV별 검사 이력 조회** — 선택한 시리얼번호의 모든 검사 기록이 시간순(최신 먼저)으로 나열된다. 각 검사 건을 펼치면 5개 화면의 OK/NG, 불량 항목, 조치 메모를 확인할 수 있다.
7. **TV 목록에서 삭제(숨김)** — 목록에서 TV를 체크박스로 개별 선택해 "삭제"할 수 있다. "삭제"는 해당 TV와 그 검사 이력을 실제로 지우는 것이 아니라 기본 목록에서 제외하는 것이며, 원본 기록은 그대로 남아 "삭제된 TV 목록" 화면에서 계속 조회할 수 있다(§6 결정 기록 참고). 전체 일괄 삭제는 제공하지 않는다.

---

## 3. 데이터 구조

검사 기록 한 건(=검사 세션 1회)은 아래 3개 단위로 구성된다.

### 3.1 TV

| 필드명 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `serial_number` | string | ✔ | TV 고유 식별자 (기본 키) |
| `model_name` | string | ✔ | TV 모델명 (최신 검사 시점 값으로 갱신) |
| `created_at` | datetime | 자동 | 최초 등록(최초 검사 저장) 일시 |

### 3.2 Inspection (검사 세션 — 검사 기록 한 건)

| 필드명 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `id` | string/number | 자동 | 검사 건 고유 ID (기본 키) |
| `tv_serial_number` | string | ✔ | 대상 TV (TV.serial_number 참조) |
| `model_name` | string | ✔ | 검사 시점 모델명 스냅샷 |
| `inspector_name` | string | ✔ | 검사자 이름 (2026-08-14부터: 등록된 검사자 명부에서 선택, 저장 시 서버가 그 시점의 명부 값으로 확정 — §6 결정 기록) |
| `inspector_id` | string | ✔ | 검사자 사번 (등록된 검사자만 선택 가능 — 미등록 사번으로는 저장되지 않는다) |
| `inspector_contact` | string | ✔ | 검사자 연락처(전화번호 등, 등록된 검사자 명부 값을 그대로 기록) |
| `inspected_at` | datetime | 자동 | 검사(저장) 일시, 서버에서 기록 |
| `overall_result` | "OK" \| "NG" | 자동 계산 | 하위 screen_results 5건 중 하나라도 NG면 NG, 전부 OK면 OK |

### 3.3 ScreenResult (화면 판정 — Inspection 1건당 정확히 5개)

| 필드명 | 타입 | 필수 | 설명 |
|---|---|---|---|
| `id` | string/number | 자동 | 화면 판정 고유 ID |
| `inspection_id` | string/number | ✔ | 소속 검사 건 (Inspection.id 참조) |
| `screen` | "White"\|"Red"\|"Green"\|"Blue"\|"Black" | ✔ | 검사한 화면 (한 Inspection 내 중복 불가) |
| `result` | "OK" \| "NG" | ✔ | 해당 화면의 정상/불량 판정 |
| `defect_types` | string[] (픽셀 이상 \| 줄 이상 \| 국소적 색 이상 \| 화면 미출력) | NG일 때만 ✔ (1개 이상) | result=NG일 때 1개 이상 선택, OK면 빈 배열 |
| `note` | string | NG일 때만 ✔ | 조치 메모/특이사항. result=NG면 필수, OK면 선택 |

---

## 4. 제약

- NG로 판정한 화면은 **조치 메모가 없으면 저장되지 않는다.**
- NG로 판정한 화면은 **불량 항목을 1개 이상 선택하지 않으면 저장되지 않는다.**
- 모델명, 시리얼번호, 검사자 이름, 검사자 사번, 검사자 연락처 중 하나라도 비어 있으면 저장되지 않는다.
- White/Red/Green/Blue/Black **5개 화면 판정이 모두 입력되어야** 저장된다. 하나라도 누락되면 저장되지 않는다.
- **저장된 검사 기록은 수정·삭제할 수 없다.** 같은 TV를 재검사하면 새로운 Inspection 건이 추가될 뿐, 기존 기록은 그대로 남는다.
- (MVP 범위 밖) 향후 오타 수정이 필요해지면 기존 기록을 덮어쓰지 않고 수정 이력을 남기는 방식으로만 확장한다.

---

## 5. 완료 조건

브라우저에서 아래를 모두 확인할 수 있으면 완료로 본다.

1. 검사 입력 화면에서 모델명·시리얼번호·검사자 이름·사번·연락처와 5개 화면(White/Red/Green/Blue/Black) 판정을 입력하고 저장할 수 있다.
2. NG로 표시한 화면에 불량 항목이나 조치 메모를 비운 채 저장을 누르면 저장이 거부되고, 어떤 항목이 문제인지 화면에 표시된다.
3. 5개 화면 중 하나라도 판정을 입력하지 않고 저장을 누르면 저장이 거부된다.
4. 저장에 성공하면 해당 TV가 검사 TV 목록에 나타나고, 모델명·시리얼번호·최근 검사일·최근 검사 결과(OK/NG)·검사자·총 검사 건수를 확인할 수 있다.
5. 목록에서 TV를 클릭하면 해당 시리얼번호의 모든 검사 이력이 시간순(최신 먼저)으로 표시되고, 각 건에서 5개 화면의 OK/NG·불량 항목·조치 메모를 확인할 수 있다.
6. 같은 시리얼번호로 검사를 여러 번 저장해도 이전 검사 이력이 사라지지 않고 그대로 누적되어 조회된다.
7. lint / test / verify가 모두 PASS한다.

---

## 6. 결정 기록

BRIEF.md 작성 시점에는 명시되지 않았거나, 서로 다른 해석이 가능했던 지점을 사용자와 협의해 아래와 같이 확정했다.

| 결정 사항 | 확정 내용 | 이유 |
|---|---|---|
| 검사 기록 저장 단위 | 검사 세션 단위 (TV정보+검사자 1회 입력 + 5개 화면 판정을 묶어 검사 1건으로 저장) | BRIEF의 목표 흐름도("TV 정보 입력 → 화면별 육안 검사 → … → 검사 결과 저장")가 세션 단위 흐름을 전제로 하고, 이력 예시 텍스트는 화면별 표기였음. 두 표현이 충돌해 사용자에게 확인했고, 목표 흐름과 자연스럽게 맞는 세션 단위로 결정. 이력 화면은 세션 1건 + 화면별 상세 펼쳐보기로 재구성. |
| 화면 입력 필수 여부 | White/Red/Green/Blue/Black 5개 화면 판정 모두 입력해야 저장 가능 | 실제 검사 절차가 5가지 단색 화면을 모두 순차 검사하는 것이므로, 일부만 입력하고 저장되면 "누락된 검사"가 완료된 것처럼 보일 위험이 있음. BRIEF가 해결하려는 불편사항("불량 항목 누락… 사람이 다시 확인해야 한다")과 직결되어 5개 전부 필수로 결정. |
| 불량 항목 선택 방식 | 화면 1개에서 불량 항목(픽셀 이상/줄 이상/국소적 색 이상/화면 미출력) 복수 선택 가능 | 한 화면에서 여러 불량이 동시에 나타날 수 있는 실제 상황(예: 픽셀 이상 + 줄 이상 동시 발생)을 표현하지 못하면 기록 누락으로 이어짐. 데이터 모델에 배열로 저장하는 비용이 크지 않아 복수 선택을 기본으로 채택. |
| NG 조치 메모 단위 | NG로 판정된 화면마다 개별 메모 입력 | 한 검사에서 여러 화면이 동시에 NG인 경우, 메모를 세션 전체에 하나만 두면 "어떤 화면의 어떤 문제에 대한 메모인지" 구분이 안 됨. BRIEF가 강조하는 "화면별로 어떤 불량이 발견됐는지 기록"이라는 목적과 일치하도록 화면별 개별 메모로 결정. |
| 검사자 정보 입력 방식 | 계정(로그인)을 만들지 않는다. 검사자 이름·사번은 사전 등록 없이 사용자가 매 검사마다 자유롭게 입력 — 사실상 "계정을 사용자 임의로 추가"하는 것과 같은 효과를 자유 텍스트 입력으로 구현 | 로그인/계정 연동은 BRIEF·기존 SPEC에서 이미 제외 대상으로 명시됨(§1 목적, 인증 없음). 실운영 인증이 아니라 사내망에서 빠르게 기록하는 것이 목적이므로, 검사자 목록을 별도로 관리하지 않고 그때그때 입력한 값을 그대로 기록에 남긴다. |

| 검사자 연락처 저장 여부 | 검사자 연락처(전화번호 등)를 필수 항목으로 신규 추가해 저장한다 | NG 후속 조치 시 검사자에게 바로 연락할 수 있어야 한다는 요구로 추가됨. 최초에는 개인정보 최소화를 위해 저장하지 않기로 했으나(.claude/rules/점검기록-데이터.md 초안), 사용자 확인 후 사번과 같은 방식(로그인 계정 없이 자유 입력, 필수 저장)으로 변경. |
| TV 목록 "삭제" 기능의 동작 방식 | 실제 행 삭제(DELETE/UPDATE)가 아니라, 별도의 append-only `tv_deletion` 테이블에 "삭제됨" 기록만 추가하고 기본 목록 조회 시 제외한다. `tv`/`inspection`/`screen_result`/`screen_note_correction` 원본 행은 절대 수정·삭제하지 않는다. | CLAUDE.md·BRIEF.md 모두 "검사 기록은 추가만 하고 수정·삭제하지 않는다"를 핵심 요구사항으로 명시하는데, 목록이 지저분해져 개별 항목을 치우고 싶다는 요구가 별도로 생김. 두 요구를 동시에 만족시키기 위해 "숨김 처리 + 삭제된 목록 화면에서 계속 조회 가능"으로 결정. |
| 검사자 정보(이름/사번/연락처) 저장소를 SQLite → Supabase(Postgres)로 이전 (2026-08-14) | BRIEF.md §"이번에는 하지 않는 것"의 "DB 계정 연동/사내 계정 로그인 연동 없음" 및 구 CLAUDE.md "외부 DB나 사내 계정 로그인을 연동하지 않는다" 규칙에 대한 예외를, 사용자가 명시적으로 승인해 확정. 로그인/계정 시스템(인증)은 여전히 추가하지 않는다 — 검사자 이름·사번·연락처는 지금처럼 매 검사마다 자유 입력을 받되, 그 값을 저장하는 위치만 로컬 SQLite `inspection` 테이블 컬럼에서 Supabase `inspector` 테이블(검사 건당 1행, `inspection_id`로 매칭)로 옮긴다. TV/검사/화면판정(`tv`/`inspection`/`screen_result`/`screen_note_correction`)은 그대로 로컬 SQLite에 남는다. | 사용자가 "사용자 관리를 Supabase DB로 관리"를 명시적으로 요청했고, 기존 규칙과의 충돌을 확인시킨 뒤 "외부 DB 금지 규칙 자체를 삭제"하기로, 범위는 "자유 입력 검사자 정보를 SQLite 대신 Supabase에 저장"으로 각각 확정받았다(계정/로그인 도입은 명시적으로 선택하지 않음). 이 결정에 따라 CLAUDE.md의 해당 금지 문구를 삭제했다. 부작용: 이제 백엔드는 기동 시 Supabase 자격증명(`backend/.env`의 `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`)이 반드시 있어야 하며, 순수 로컬 SQLite만으로는 검사자 정보가 필수 항목인 검사 저장·조회 흐름을 시연할 수 없다 — BRIEF.md §"로컬 DB 또는 샘플 데이터 저장소만으로 시연 가능해야 한다"는 전제가 이 범위에서만 깨졌음을 알고 있는 상태로 승인받았다. |
| 검사자 "사전 등록 없음" → "사전 등록 필수 선택"으로 전환 (2026-08-14) | 검사 입력 화면의 검사자 이름/사번/연락처 자유 텍스트 3칸을, Supabase `registered_inspector` 테이블(살아있는 명부 — insert/update/delete 모두 허용, `employee_id` 유니크)에서 고르는 단일 select로 교체한다. 새 "검사자 등록" 화면(프론트 `/inspectors`)에서 이름/사번/연락처를 추가·수정·삭제할 수 있다. 검사 저장 시 서버가 클라이언트가 보낸 이름/연락처를 신뢰하지 않고, `inspector_id`(사번)로 `registered_inspector`를 조회해 그 시점의 이름/연락처를 그대로 검사 이력(`inspector` 테이블)에 기록한다 — 등록되지 않은 사번이면 저장을 거부한다(400). 이 결정으로 위 결정 기록의 "검사자 정보 입력 방식"(계정 사전 등록 없음)과 LB-125의 완료 조건은 이 시점부터 대체된다. 단, 이미 저장된 과거 검사 이력(자유 입력 시절 데이터)은 append-only 원칙에 따라 그대로 두고 고치지 않는다. | 사용자가 "사용자 등록 페이지를 만들어서 supabase에 넣어주고 검사이력을 넣을 때 사용자 입력을 supabase에 넣어져 있는 사용자만 선택되도록 바꿔줘"라고 명시적으로 요청. 기존 SPEC 결정 및 LB-125 완료 조건과 정면 충돌함을 사용자에게 확인시킨 뒤, "등록된 검사자 수정/삭제 허용"·"선택 시 이름+사번 함께 표시"로 세부 사항을 확정받아 진행했다. |

파생 결정 (위 4가지의 직접적 귀결, 별도 협의 없이 BRIEF 본문 근거로 확정):

- TV 목록의 "최근 검사 결과"는 최근 Inspection의 5개 화면 판정 중 하나라도 NG면 NG, 전부 OK면 OK로 집계한다.
- TV 목록의 "총 검사 건수"는 해당 TV(시리얼번호)에 누적된 Inspection 전체 건수다 (이력이 여러 건인데 목록에서 드러나지 않아 불편하다는 피드백에 따라 추가).
- NG 화면은 조치 메모뿐 아니라 불량 항목도 1개 이상 선택해야 저장된다 (BRIEF가 "불량 항목 누락"을 명시적 불편사항으로 언급함).
