# TV 화면 검사 이력 관리 웹앱

**Capstone 트랙**: 트랙 A — 풀스택 웹앱 (하네스는 ②에 도구로 정리)
**구현에 쓸 수 있는 시간**: 코칭 전후·저녁 + 다음 날 발표 전까지 (실질 6~8시간)

## ① 문제 정의 — 지금 무엇이 불편한가
- 누가 / 언제 / 어떤 일에서: TV 화면 품질 검사자가 White/Red/Green/Blue/Black 화면을 육안 검사하고 결과를 시트/수기로 기록할 때, 불량 항목 누락이나 NG인데 조치 메모가 없는 상태를 사람이 다시 확인해야 함
- 줄이는 비용 (반복 / 대기 / 검증 중 하나): 검증
- 지금 걸리는 시간·횟수: 하루 평균 1000대 검사, 과거 이력 하나 찾는 데 1분 걸림

## ② AI 에이전트에게 맡길 부분
- 에이전트가 대신할 일: 코드 작성
- 사람이 계속 할 일: 화면 디자인 결정, 최종 승인
- 쓸 도구 (서브에이전트·MCP·스킬·hook·병렬 중 필요한 것만): 서브에이전트, 스킬(plan-contract), hook(PreToolUse로 backlog.json 직접 수정 차단, Stop으로 lint/build/typecheck/300줄 검사)

## ③ 범위 — 반드시 / 되면 좋은 / 안 하는 것
- 반드시 (이게 안 되면 실패): ① 검사 입력 및 저장(모델명/시리얼번호/검사자 정보/화면별 판정/불량 항목/조치 메모, NG인데 메모 없으면 저장 차단) ② 검사 TV 목록 조회 ③ TV별 검사 이력 시간순 조회
- 되면 좋은: 모델명/시리얼번호 검색, OK/NG 필터, NG 결과 눈에 띄게 표시(빨간색 등), 검사 결과 수정 이력 저장, 월별 엑셀 다운로드
- 이번엔 안 하는 것: DB 계정 연동, 사내 계정 로그인 연동, 실제 TV 화면 색상 자동 제어, 복잡한 관리자 페이지, 담당자 자동 알림, 실운영 수준 인증/권한 관리

## ④ Phase 분할과 각 Phase 완료 기준
1. 검사 입력 및 저장 — 완료: 모델명/시리얼번호/검사자/검사 화면/불량 항목을 입력·저장할 수 있고, NG인데 조치 메모 없으면 저장 실패하며, 저장된 데이터가 다시 조회된다 (다른 Phase보다 범위가 크다는 걸 인지하고 그대로 유지하기로 함 — 백엔드/프론트 초기화, DB 스키마, 5개 화면 UI, 저장 API, 검증 로직을 모두 포함)
2. 검사 TV 목록 — 완료: 저장된 TV가 API로 조회되어 목록에 표시되고, 모델명/시리얼번호/최근 검사 결과가 보이는 화면을 사용자에게 보여주면 사용자가 직접 보고 승인한다 (사람 승인 게이트 — ⑤ 참고)
3. TV별 검사 이력 — 완료: TV 선택 시 동일 시리얼번호의 과거 검사 기록이 시간순으로 표시되며, `npm test`가 통과한다 — 테스트 내용: 같은 시리얼번호로 이력을 여러 번 입력한 후 조회했을 때 입력한 이력이 모두 나오는지 확인

## ⑤ 완료를 판정할 방법 (검증 게이트)
- 기계가 판정하는 것: 필수 입력값 누락 시 저장 차단 / NG인데 조치 메모 없으면 저장 차단 / 정상 데이터 저장 후 재조회 확인 / 동일 시리얼번호 이력 전체 조회 확인 / lint·typecheck·build PASS(Stop 훅으로 강제) / `npm test` PASS
- 사람이 눈으로 보는 것: 검사 입력 화면 복잡도, White~Black 선택 명확성, NG 결과가 눈에 띄는지, TV 목록에서 원하는 TV를 쉽게 찾는지, TV 선택→이력 조회 흐름이 자연스러운지. **Phase 2는 이 확인 자체가 완료 판정 게이트**(TV 목록 화면을 보여주면 사용자가 보고 승인)
- 내가 직접 승인할 지점: git commit/push 전, npm 의존성 추가(package.json 변경) 전, GitHub 원격 저장소 최초 생성/최초 push 전에는 항상 사용자 승인을 받는다

## GitHub 저장소 URL
https://github.com/swordstory85-max/INSPECTION-LAB

## 1:1 코칭에서 가장 묻고 싶은 것
"고칠 곳" 1~3번 전부: Phase 1 범위가 너무 큰지, Phase 2 완료 기준을 자동화 테스트로 어떻게 묶을지, git commit/push 외에 어떤 작업에 승인 지점을 더 둬야 하는지.

---
(아래는 폼에 넣지 않는 작업 메모)

## 되돌아갈 지점과 데이터
- 되돌리는 방법: git 저장소 초기화 후 시작 커밋(7b29f91, "Initial checkpoint")을 남김. 문제가 생기면 `git reset --hard 7b29f91` 또는 `git log`로 커밋 확인 후 되돌린다
- 실데이터·자격증명 없이 되게 만드는 방법 (샘플로 대체할 부분): BRIEF.md "샘플 데이터"에 있는 20건(TV-65A~TV-43E 5개 모델 × 시리얼번호 SN000001~SN000020, 검사자 5명 순환)을 시드 데이터로 사용

## 고칠 곳 (4단계 반박 결과)
1. [③/④ Phase 1 범위] backlog.json 기준으로 Phase 1(검사 입력 및 저장)은 LB-101~LB-119 약 19개 항목(백엔드/프론트 초기화, DB 스키마, 5개 화면 UI, 저장 API, 검증 로직 전부)을 사실상 다 포함한다. "Phase 하나가 한두 시간"이라는 기준보다 훨씬 크고, 6~8시간 중 Phase 1에서만 대부분을 쓸 위험이 있다. → **결정: 쪼개지 않고 그대로 유지.** Phase 1이 다른 Phase보다 크다는 걸 인지한 상태로 진행하기로 함 (④에 반영).
2. [④ Phase 2 완료 기준] Phase 2 "완료:"의 "모델명/시리얼번호/최근 검사 결과를 확인할 수 있다"는 자동화 테스트에 안 묶여 있었다. → **결정: 자동화 테스트 대신 사람 승인 게이트로 확정.** 웹앱 화면을 사용자에게 보여주면 사용자가 직접 보고 승인하는 방식으로 통과 판정 (④·⑤에 반영).
3. [⑤ 승인 지점] "git commit/push 전 승인"만 명시돼 있었다. → **결정: 반영 완료.** npm 의존성 추가, GitHub 원격 저장소 최초 생성/최초 push를 승인 지점에 추가함 (⑤에 반영).

<!-- 기획 시작 13:15 -->
